# Island Flask App
## Richard Comins

# local laptop
pip install flask      # done
flask run

# on other machines
use virtual environment

# if it stops working on laptop
Ctrl-Shift-P (command pallete)
"Select Python Interpreter"
pick the (global) one